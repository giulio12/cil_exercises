function a = lassign(X, mu0, Sigma0, mu1, Sigma1)

prob1 = 1/(2*pi)^(size(X,1)/2)


end

% a = g(X, mu0, sigma0 < g(X, mu1, Sigma1);
% a = a + 1

% function p = g(X, mu, Sigma)
% D = (X - repmat(mu,1,size(X,2))';
% p = -log(det(Sigma)) - sum(D/Sigma).*D,2)';
% end
